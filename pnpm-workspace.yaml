allowBuilds:
  "@parcel/watcher": true
  # testcontainers' docker client only needs the local unix socket here; the
  # native ssh2 crypto bindings are for connecting to a remote docker host.
  cpu-features: false
  esbuild: true
  protobufjs: false
  ssh2: false
